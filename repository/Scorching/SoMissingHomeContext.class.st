"
I am triggered when trying to inline a block in its home context, but the home context is too far or not on stack.
"
Class {
	#name : #SoMissingHomeContext,
	#superclass : #Error,
	#category : #'Scorching-Error'
}
