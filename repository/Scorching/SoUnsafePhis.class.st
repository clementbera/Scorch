"
I am signalled when a case not supported by the optimiser happens: when in a loop, a phi refers to another phi from the previous iteration.
"
Class {
	#name : #SoUnsafePhis,
	#superclass : #Error,
	#category : #'Scorching-Error'
}
