"
This error has happened while executing the unoptimized or the optimized method and that an error is raised (it can imply that the optimized method is incorrect)
"
Class {
	#name : #SoRuntimeComparisonError,
	#superclass : #SoValidationFailure,
	#category : #'ScorchingTests-Validation'
}
