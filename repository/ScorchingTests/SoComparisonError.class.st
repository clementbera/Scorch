"
This error has happened while comparing the result and effect of the unoptimized method with the optimized method. 

This usually implies that the optimizing compiler has generated an optimized method without errors, but the generated optimized method is incorrect.
"
Class {
	#name : #SoComparisonError,
	#superclass : #SoValidationFailure,
	#category : #'ScorchingTests-Validation'
}
