"
Happens during comparison of method vs optimized method, typically when the optimized method ends up in an infinite loop due to incorrect optimized code generation
"
Class {
	#name : #SoTimeOut,
	#superclass : #SoValidationFailure,
	#category : #'ScorchingTests-Validation'
}
